load ../gemm.maude

mod GEMM-TEST is 
    protecting GEMM .
    protecting COMPUTE-TEST .
    var comp : Computation .
    var b : Bool .
endm

search [10, 10] in GEMM-TEST : compute(v, X Y + Z with no-knowledge)
        =>! comp such that valid(comp).
